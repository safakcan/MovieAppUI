# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MovieAppUI' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MovieAppUI
  pod 'MADNetwork', :git => 'https://github.com/teammadduck/MADNetwork'
  pod 'Kingfisher', '~> 7.0'
 
 target 'MovieAppUITests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MovieAppUIUITests' do
    # Pods for testing
  end

end
